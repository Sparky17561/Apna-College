1)To make a website responsive we give it a max width and when the width of website hits the max width or lower then apply the property
called flex-wrap : wrap ......it will wrap the flexbox elements and will be responsive.

Javascript:
case-sensitive
+++++++++++++++++++++++++++++++++++++++++++++++++
Console Uses REPL (Read-Evaluate-Print-Loop)
To clear -> CTRL + L 

+++++++++++++++++++++++++++++++++++++++++++++++++

typeof  (varname) --> shows datatype
2**3 = 8 (exponential)

NaN -> not a number (ex- 0/0)
+++++++++++++++++++++++++++++++++++++++++++++++++

1) let (keyword) -> syntax of declaring variables
----------------------------------------------
ex- 

let b; (variable "b" defined)

console.log(b) (we will get undefined)
-----------------------------------------------

2) const (keyword) -> values of constants can't be changed
with re-assignment and they can't be re-declared

------------------------------------------------

ex- const year = 2025

year = 2026 #error
year = year + 1 #error
------------------------------------------------

3) var (also used for variables but is old .... let replaced var)

++++++++++++++++++++++++++++++++++++++++++++++++++


Typescript is more stricter version of Javascript

ex- in JS -> a = 5 then a = "true" .....accepted
    in TS -> a = 5 then a = "true" .....rejected
    once defined as a number always a number
    JS is dynamic , TS is static


++++++++++++++++++++++++++++++++++++++++++++++++++++
Strings

.length --> used to get length of string
 + --> concatenation


 undefined --> value is not assigned and we are trying to print it
 null --> intentionally value not assigned

 "apnacollege" + 123 --> input (string + number)
 "apnacollege123" --> output (string)

++++++++++++++++++++++++++++++++++++++++++++++++++++

console.log(message) --> used to output a message

Template Literals

let a = 10;
let b = 5;
console.log(`sum is ${a+b}`);

output --> 15

"==" checks value
"===" checks value and type (strict comparison)

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

alert("something is wrong") --> gives an alert

let name = prompt("Enter your name") --> gives a text field in alert and stores in name 


console.error() --> used for error

console.warn() --> used for warning

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


strings are immutable in JS

1) .trim() --> makes a new string which removes whitespaces from string it is used on
2) .toUpperCase() --> self explained
3) .toLowerCase() --> self explained
4) .indexOf() --> returns the first index of occurence of some value in string or gives -1 if not found


method chaining -> to use multiple methods on a string


.slice(start,end) --> returns a part of the original string -->end is exclusive

.replace("old","new") --> only first occurence is changed

.repeat(n) --> repeats the string n times


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Array (Data Structure)


let num =[2,3,4,5] --> square brackets are used
num[0] --> outputs "2"

typeof num --> returns object

let arr = [] --> accepted
let arr = ["hi",1,"how r u",7] --> accepted

.length --> used to get length for Array

arr[0][0] --> outputs 'i'

Arrays are mutable

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Array methods

.push(element) --> appends element in the last of the array
.pop() --> returns last element in array and removes the last element

.unshift(element) --> appends element in the start
.shift() --> returns the first element in the array and removes the first elements 

.indexOf(element) --> returns the index of that element from the array

.concat() --> used to merge 2 Arrays

ex - Array1.concat(Array2) 

.slice() --> works here also (same as string )

.splice(start,deleteCount,item0.....itemN) -> removes / replaces / adds element (all in one)

.sort() --> used for sorting in ascending alphabet

.reverse() --> used to reverse the array


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Array referencing --> each array has different address

if we compare arr1 == arr2 then it will return false bcaz both have different address (irrespective of elements)

but if we do arr2 = arr1 and then compare them it will return true as the address is also given to array 2 with elements


if we do const arr = [1,2,3]; 

value can be changed but address cannot override

ex - arr = cars will give error

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Nested Arrays --> visualize as matrix

Arrays of Arrays

ex - arr = [[1,2],[5,6]]

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


for Loop

ex - for(let i=1; i<=5; i++)
        {
            console.log(i)
        }


while loop

ex - while(condition)
    {
        // do something
    }


for of loop

ex- for (element of collection)
    {
        // do something
    }


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

JS object Literals --> used to store keyed collections and complex entitites

does not have a specific order 

ex - let student = {
        name : "sai",
        age : 23,
        marks : 95,
        city : "Mumbai"
};

How to get our values?

1. use student["name"]
2. use student.name

to update just write student.name = "new name"
to add -> student.newparameter = "value"
to delete -> delete student.name


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

We can also do nesting

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

We can make array of objects

arr = [
    {
        name:"sf",
        age:"12"
    },
    {
        name:"sdfwed",
        age:34
    },
    {
        name:"wef",
        age:90
    }
];

Actions same as Objects

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


Math Function

Properties ->
Math.PI 
Math.E

Method ->
Math.abs()
Math.pow(a,b)
Math.floor()
Math.ceil()
Math.random()


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


JS Functions 


let arr=[8,9,10,1,2,3,4,5,6,7];
let num=5;

//elements larger than a number num 

function getElements(arr,num)   -> this is how u write fn name
{
    for(let i=0;i<arr.length;i++)
    {
        if(arr[i]>num)
        {
            console.log(arr[i]);
        }
    }
}

getElements(arr,num); --> this is how u call a function


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++



"this" keyword :

this keyword refers to an object that is executing the current piece of code

a function cannot access the parameters out of its domain


ex -

const student = {
    name : 'sai',
    age: 23,
    eng:90,
    math:89,
    phy:90,
    getAvg() { ---> also a way to write a function in object .... this is called method
        let avg = (eng + math + phy)/3;
        console.log(avg);

    }
}



now if we call

student.getAvg() --> will give an error as the function is not able to see eng , phy , math out of its domain

instead we replace the line with this 

"let avg = (this.eng + this.math + this.phy)/3"

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


Try and catch - used in exceptions


try {
    console.log(a)
}catch {
    console.log("variable a doesnt exist")
}


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

() => {}; --> this is arrow function


Arrow function (shortest way to declare a fn)
These are nameless function 

const func = (arg1,arg2) => {
    function definition
};



example -> const sum = (a,b) => {
    console.log(a+b);
};


arrow function with implicit return -> it returns value without using return statement
althought there is change in the syntax then 

const func = (arg1,arg2) => (value);

ex- const mul = (a,b) => (
    a*b;
);



+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Set Timeout 

setTimeout(function,timeout)

function -> ur function
timeout -> delay in ms 


ex - 

setTimeout(() =>{
    console.log("Hello world");
},4000);

delays function for 4 sec 

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


Set Interval

setInterval(function, timeout)

recursively runs function within the timeout Interval


setInterval(() =>{
    console.log("Hello world");
},2000);


infinietly runs after 2 sec



to stop a interval we store function in a variable and use clearInterval();


ex - 

const id = setInterval(() => {
    console.log("Hello");
},2000);


=> clearInterval(id); -> stops the infinite process


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++=


This with arrow function 

normal function has scope depending on the object it is been called by

arrow function has lexical scope (jitna parent ka utna iska)


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


Array methods


- forEach

ex. 

let arr = [1,2,3,4,5]; 


arr.forEach(function(el){
    console.log(el);
});

arr.forEach((el) => {
    console.log(el);
});

// prints each and every element

-------------------------------------------------------------------------

Map - similar to forEach but it computes for every element and then returns the value in a new array

needs return statement
ex- 

let num = [1,2,3,4,5];

let double = num.map((el) => {
    return el*el;
});




ex - (for objects)


let students = {
    {
        name: 'aman',
        marks: 96,
    },
    {
        name: 'sai',
        marks: 90,
    }
};


let gpa = students.map((el) => {
    return el.marks/10;
});


-------------------------------------------------------------------------


Filter

if the answer is true it adds in the array else if false it ignores


let nums = [1,2,3,4,5];

let ans = nums.filter((el) => {
    return el%2 == 0;
})


-----------------------------------------------------------------------------------


Every 

Returns true if every element of array gives true for some functions. Else returns false

AND gate

ex- 

[2,4,6].every((el) => (el%2==0));

-------------------------------------------------------------------------------------

Some 

Returns true if some elements of array give true for some function else returns false

opposite of Every ==> OR gate

-----------------------------------------------------------------------------------------------

Reduce 

Reduces the array to a single value

maybe not a boolean value


arr.reduce(accumulator,element);


ex-

[1,2,3,4].reduce((res,ele) => (res+ele));

iter 1: 
ex- acc=2
ele = 1

res = 1+2 = 3

iter 2: 

acc=res=3
ele=2

res/acc = 3+2=5

iter 3: 
acc=5
ele=3

res/ele = 5+3 =8

iter 4: 

acc=8
ele=4

res=8+4=12

iter 5: no ele left

returned value is 12

------------------------------------------------------------------------------------


Default parameters

Giving a default value to the argument


ex. 

function sum(a,b=3) {
    return a+b;
}

sum(2); //5

sum(3,4); //gives correct 7

if a=2 and b is variable

and we call sum(1);  -> it will give error unidentified since first argument given will be for a and not b

------------------------------------------------------------------------------------------


Spread

Expands an iterable into multiple values

used by three dots ...

normally we use

Math.min(1,2,4);

and we get 1 as answer


but what if we get an array from which we have to check 

like arr = [1,2,4];

for this either we can write

Math.min(arr[0],arr[1],arr[2]);

else

Math.min(...arr);


works on strings aswell


console.log(...'hello')


output: h e l l o

we can also do 


let odd = [1,3,5,7];

let even = [2,4,6,8];

let nums = [...odd,...even]; -> this makes an array of  odd numbers first and then even



Spread with Object Literals

function mergeObjects(arr,arr2){
    let object = {...arr,...arr2};
    console.log(object)
}
=-----------------------------------------------------------------------------------------


Rest 

opposite of Spread

Allows a function to take an indefinite number of arguments and bundle them in an Array


ex . 

function sum(...args) {
    for(let i=0; i<=args.length, i++){
        console.log('you gave us : ', args[i]);
        let total = 0;
        total += args[i];
    }
}


-----------------------------------------------------------

Destructuring

Storing values of array into multiple variables

let names = ['tony','bruce','petet'];
let winner = names[0];
let runnerup = names[1];
let secondRunnerup = names[2];

---OR---

let [winner,runnerup,secondRunnerup] = names;

----------------------------------------------------------------------
we can also combine Destructuring with Rest

let [winner, runnerup , secondRunnerup , ...others] = names -> if names contain more than 3 names , from 4th onwards it will be inside the others array


------------------------------------------------------------------------------

for Objects Destructuring


const student = {
    name: 'karan',
    age: 14,
    class:9,
    subjects: ['hindi','eng','science','maths'],
    username: 'sai',
    password: '1234',
};


let {username, password} = student;

will give username and password of that student


we can also give new names to the variables by using:

let {username: user, password: secret, city: place= 'mumbai'} = student; --> we can also take default values
--------------------------------------------------------------------------------------

DOM (Document Object Model)

The DOM represents a document with a logical three

It allows us to manipulate/change webpage content (HTML elements)


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


Selecting elements

getElementById

Returns the element as an object or null (if not found)

-----------------------------------------------------------------------------------------

getElementByClassName

Returns the element as an HTML collection or empty collection (if not found)
ex. 

let smallImages = document.getElementsByClassName('oldImg');

for(let i=0;i < smallImages.length;i++ )
{
    smallImages[i].src = 'image.png';
    console.log('value')
}

changes all images to a default image

----------------------------------------------------------------------------------------

getElementByTagName

Returns the element as an HTML collection or empty collection (if not found)

ex.
document.getElementByTagName('p');


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


Query Selectors

Allows us to use any CSS selector

used to select single element which appears first

-----------------------------------------------------------------------

QuerySelectorAll

For all the elements
document.querySelectorAll('p');

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


using properties and methods

innerText

Shows the visible txt contained in a node

----------------------------------------------------
textContent

Shows all the full text (hidden also)
------------------------------------------------------------
innerHTML

shows the full markup
------------------------------------------------------------


Manipulating Attributes

obj.getAttribute(attr) --> getters

obj.setAttribute(attr,val) --> setters

- setAttribute isnt used much cause it only assignes single value 

----------------------------------------------------------------


Manipulating Style

style property

obj.style


ex. 

let links = document.querySelectorAll('.box a');

for(let i=0; i<links.length; i++){
    links[i].style.color = 'purple';
}

--> changes color to purple for all links
--> but we dont usually use this method
--> we can only see inline css written in html through this

-------------------------------------------------------------------------


using classList

obj.classList


we have to first create classes then we can use empty

classList.add() to add new classes

classList.remove() to remove classes

classList.contains() to check if class exists

classList.toggle() to toggle between add and remove -> set and reset and also return true or false for the class if exists or not


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Navigation

parentElement

children

.childElementCount --> returns the no. of children

.previousElementSibling / .nextElementSibling


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Adding elements

document.createElement('p') --> used to add new paragraphs

after adding a new element we can set a new value to it by using innerHTML , innerText

but then it wont be shown in screen ; for which we will use append functions

.appendChild(element) --> ex- body.appendChild('p') --> appends at last of any parent

append(element) --> we can append text, string --> appending in an existing element - ex - newP.append('this is added text')

prepend(element) --> added in first to the para - ex - newP.prepend('first text')


insertAdjacent(where,element) -> ex . p.insertAdjacent('beforeend',btn)

types 

.insertAdjacent('beforebegin',btn)
.insertAdjacent('afterbegin',btn)
.insertAdjacent('beforeend',btn)
.insertAdjacent('afterend',btn)


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


remove

removeChild(element) acts like appendChild but removes

element.remove()


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

DOM events


Events are signals that something has occured. (user inputs / actions )

Inline events (multiple function wont execute): 
button - onclick - when an element is clicked

ex. 

let btn = document.querySelector('button');
console.dir(btn);

btn.onclick = function(){
    alert('btn was clicked');
};


OR 

function sayHello(){
    alert('hello');
};

btn.click = sayHello;


FOR MULTIPLE BUTTONS

let btns = document.querySelectorAll('button');

for(btn of btns){
    btn.onclick = sayHello;
}

onmouseenter (when mouse hovers on an element)


ex- 

btn.onmouseenter = function(){
    console.log('u entered a button');
}

-----------------------------------------------------------------------------


Event Listener (multiple function works)

addEventListener

element.addEventListener(event,function/callback);


ex. 
for (btn in btns){
btn.addEventListener('click',sayHello);
btn.addEventListener('click',sayName);
}

multiple functions executed at a time is possible using for loop and addEventListener


----------------------------------------------------------------------------------------


this in Event listener 

When 'this' is used in a callback of event handler of something. it refers to that something


ex. 

h1.addEventListener('click',function(){
    this.style.backgroundColor = 'pink';
})

here this means h1

this is used to avoid redundancy

lets say 4 elements have the same function to change a color we make a generic function

function changeColor(){
    this.style.backgroundColor = 'blue';
}

ele1.addEventListener('click',changeColor);
ele2.addEventListener('click',changeColor);
ele3.addEventListener('click',changeColor);
ele4.addEventListener('click',changeColor);

-------------------------------------------------------------------------------------------------------


Keyboard events

let inp = document.querySelector('input');

inp.addEventListener('keydown',function(){
    console.log('key was pressed')
})

inp.addEventListener('keyup',function(){
    console.log('key was released')

})

key.code is important here check directory for it
---------------------------------------------------------------------------------------


Forms event

let form = document.querySelector('form');

form.addEventListener('submit', function(){
    alert('form submitted');
});


event.preventDefault() --> wont redirect to default page which fails


-----------------------------------------------------------------------------------



form.addEventListener('submit', function(){
    event.preventDefault();


    let inp = document.querySelector('input');
    console.dir(inp);
    console.log(inp.value); --> to track the input value

});


LOGIN


form.addEventListener('submit', function(){
    event.preventDefault();


    let user = document.querySelector('#user'); -> id='user'
    let pass = document.querySelector('#pass'); -> id='pass'
    
    console.log(user.value); --> to track the username 
    console.log(user.value); --> to track the password

    alert('Hi ${user.value}, your password is set to ${pass.value}');

});


OR (without using query selector)


form.addEventListener('submit', function(){
    event.preventDefault();


    let user = this.element[0];
    let pass = this.element[1];

    console.log(user.value); --> to track the username 
    console.log(user.value); --> to track the password

    alert('Hi ${user.value}, your password is set to ${pass.value}');

});


----------------------------------------------------------------------------------------

MORE Events

change event

The change event occurs when the value of an element has been changed (only works on <input>, <textarea> and <select> elements)


ex. 

form.addEventListener('submit', function(){
    event.preventDefault();
});

let user = document.querySelector('#user'); -> id='user'
    
user.addEventListener('change',function(){
    console.log('input change');
    console.log('final value = ', this.value);
}); 

sirf initial aur final changes ko track krta hai input dene k baad 



input event

the input event fires when the value of an <input>, <select> or <textarea> element has been changed

form.addEventListener('submit', function(){
    event.preventDefault();
});

let user = document.querySelector('#user'); -> id='user'
    
user.addEventListener('input',function(){
    console.log('input change');
    console.log('final value = ', this.value);
}); 

ek ek character k baad event trigger hota hai jab input me kuch kre through


----------------------------------------------------------------------------------------------------------------


Event Bubbling

nested elements pe event listener lgaya toh sabse internal wale ka trigger hua toh sab parent tk ka event execute hoga

event.stoppropagation() --> ye usko rokta hai

----------------------------------------------------------------------------------------------------------------------------

Event Delegation 

for new elements we want the old events to trigger we bring back event Bubbling


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


Call Stack LIFO used for debugging along with Breakpoint

JS is Single Threaded


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Promises

The Promise object represents the eventual completion or failure of an asynchronous operation and its resulting value

function saveDb(data){
    return new Promise((resolve,reject) =>{
        let internetSpeed = Math.floor(Math.random()*10)+1;
        if(internetSpeed < 4){
            resolve('success: data was saved');
        }
        else{
            reject('failure:weak connection');
        }
    });
}



-----------------------------------------------------------------------------

then() & catch()


let request = saveDb('Apna clg');

request
    .then(() => {
        console.log('promise was resolved');
    })
    .catch(() => {
        console.log('promise was rejected');
    });

OR


to write it more compactly

saveDb('Apna clg')
    .then(() => {
        console.log('promise was resolved');
    })
    .catch(() => {
        console.log('promise was rejected');
    });


------------------------------------------------------------------


Promise chaining -> helps in reducing bulky code

saveDb('apna clg')
    .then(() =>{
        console.log('data 1 saved');
        return saveDb('Hello World');
    })
    .then(() => {
        console.log('data2 saved');
        return saveDb('sai');
    })
    .then(() => {
        console.log('data 3 saveed');
    })
    .catch(() => {
        console.log('promises was rejected')
    });


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Async Functions

async keyword

Creates an Async Function -> returns promise jispe then() aur catch() use krskte h1


async function greet(){
    return 'Hello';
}


OR
let hello = async () => ();



greet()
    .then((result)=>{
        console.log('promise was resolved')
        console.log(result)
    })
    .catch((err)=>{
        console.log('error, promise was rejecteed')
    })



Await keyword

pauses the execution of its surrounding async function until the promise is settled (resolved or rejected)
await sirf async k andar chlta h

async function show(){
    await colorChange('violet',1000);
    await colorChange('indigo',1000);
    await colorChange('green',1000);
    await colorChange('yellow',1000);
    await colorChange('orange',1000);

    return 'done';
}


ek k baad ek function execute krna time delay k sath possible hai using async and await

but if any one of the promises got rejected in asyc then the code written after promises wont be executed as it will be in an error state


----------------------------------------------------------------------------------------------------------------------------------------


Handling Rejections in Await

using try{} and catch(){}

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


API 

Application Programming Interface

JSON

Javascript Object Notation (format)

key -> string
value -> can be anything


----------------------------------------------------

Accessing Data from JSON

JSON.parse(data) method  

To parse a string data into a JS object


JSON.stringify(json) method

To parse a JS object data into JSON


------------------------------------------------------------

AJAX

Asynchronous JavaScript and XML (API time lagata hai isiliye async process hai)

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

HTTP verbs

GET 

POST 

DELETE

------------------------------------------------

STATUS code

200 - OK

404 - Not found

400 - Bad request

500 - Internal Server error

client error -> hamne hi galat API ko request bheji
server error -> server me fault hai


------------------------------------------------------------

ADDITIONAL INFO

Query Strings

https://www.google.com/search?q=harry+potter


here q -> key
here harry+potter -> value

-------------------------------------------------------------------------


HTTP Headers --> used to give values additionally in response and request


Header , value

---------------------------------------------------------------------------------------


Our FIrst request

using Fetch

let url = 'https://catfact.ninja/fact';
fetch(url)
    .then((response)=>{
        console.log(response);
        return response.json()
        })
        .then((data)=>{
            console.log(data.fact)
            return fetch(url);
        })
        .then((res)=>{
            return res.json();
        })
        .then((data2)=>{
            console.log(data2.fact)
        })
        .catch((err)=>{
            console.log(err);
        });


fetch(url)
// async function getFacts(){
//     try{
//         let res = await fetch(url);
//         let data = await res.json()
//         console.log(data.fact)

//         let res2 = await fetch(url);
//         let data2 = await res.json()
//         console.log(data2.fact)
//     }catch(e){
//         console.log(e)
//     }
    
// }
----------------------------------------


Axios

Library to make HTTP requests

async function getFacts() {
    try{
        let res = await axios.get(url);
        console.log(res.data.fact)
    }catch(e){
        console.log(e)
    }
    
}

--------------------------------------------------

Sending Headers through Axios

const config = {headers: { Accept : "application/json"}};
let res = await axios.get(url2,config);
console.log(res.data);

-------------------------------------------------------------


Updating Query Strings 

let url = "https://universities.hipolabs.com/search?name=";

let country="nepal";


async function getColleges(){
    try{
        let res = await axios.get(url+country);
        console.log(res);
        
    }catch(e){
        console.log(e)
    }
    

}

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Terminal

Basics

ls = list files
(show files)

pwd = print working directory
(where am i?)

clear = clear screen
------------------------------------------------
Navigation

cd = change directory

cd .. = back button

cd ../.. = back 2 baar
----------------------------------------------------
Making Directories

mkdir = make directory / create folders

-----------------------------------------------------------

Flags

Flags are characters that we pass with commands to modify their behaviour

manual command 

man ls - give info about ls command

man mkdir = give info about mkdir command


with Flags

ls -l 

ls -a 

ls -la 

-------------------------------------------------------------

Deleting files and folders

rm = remove files

rmdir = remove empty folders 

rm -rf - remove any folders (recursively force delete files and then remove folder)

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Configuring Git

git config --global user.name "My name"

git config --global user.email "someone@gmail.com"

git config --list --> to check 


--------------------------------------------------------

Basic commands

clone - cloning a repo on our local machine -  git clone <link>

status - displays the state of the code - git status

add - adds new or changed files in your working directory to the FIt staging area  git add <file name>

commit - it is the record of change - git commit -m 'some message'

push - upload local repo content to remote repo - git push origin main


init - used to create a git repo - git init


commands for initialisation : 

mkdir project2

cd project 2

git init 

ls -la
--------------------------------------------------------------------------
to push a local repo to github 

1. go to github create a repo 
2. copy the link
3. enter the command
git remote add origin <link>

-------------------------------------------------

to check which branch - git master
-------------------------------------------------------------
to change name --> git branch -M main (rename to main)
------------------------------------------------------------------
git push origin main (to send local repo to github)

----------------------------------------------------


if u write git push -u origin main -> this creates an upstream 

to push next time u have to just write "git push" no need of origin main then
--------------------------------------------------------------------------------

git checkout <branch name> (to navigate)

git checkout -b <new branch name> (to create new branch)

git branch -d <branch name > (to delete branch)

git push --set-upstream origin <branch name> (to push the code to the new branch and setting an upstream)

---------------------------------------------------------------------------------------

Merging code

git diff <branch name> (to compare commits,branches , files and more )


git merge <branch name>  (to merge 2 branches)


or 

Create a pull request

Pull request - it lets u tell others about changes you've pushed to a branch in a repo on github


git pull origin main (to reflect the changes of github to ur local machine)
(after changes in github u can do this)


-------------------------------------------------------------------------------------------------------------

Merge Conflicts

if different lines of code is written on the same files of different branches these may arise while Merging

------------------------------------------------------------------------

TO ADD NEW TO existing

git add .
git status
git commit -m 'message'
git push origin main 


-------------------------------------------------------------------------------------


Fixing Mistakes

case 1: staged changes

    git reset <filename>
    git reset


case 2 : committed changes (for one commit)

    git reset HEAD~1

case 3 : committed changes (for many commits)

    git reset <commit hash> 

    use git log 

    there will be a "commit <a very long hash code>"

    then use git reset <commit hash>

    git reset --hard <commit hash> --> to delete lines of code from the editor and also reset

------------------------------------------------------------------------------------------------------------------------

What is Forking

used in open source contributions

go github > fork > give name > done 

make changes 

commit

go to pull request

original repo <- forked repo click create pull request

A Fork is a new repo that shares code and visibility settings with the original "upstream" repo

Fork is a rough copy

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


NodeJS

Javascript Runtime Environment

It is used fro server side Programming

NodeJS is not a language, library or framework

Node REPL (Read,Evaluate,Print,Loop)

enter 

node to enter the node server in cmd

enter .help for basic commands


-----------------------------------------------------------------------

Node files

Be in the directory where u have the js file to execute

type node <file name>.js in terminal to execute


module.exports

requiring Directories

require() -> a built-in function to include external modules that exist in seperate files

module.exports -> a special object


------------------------------------------------------------------------


suppose we have a root directory

inside that we have a fruits directory and script.js

inside fruits we have 3 files 

apple.js
banana.js
orange.js


inside which the code is written

// apple.js
module.exports = {
    name: 'apple',
    color:'red'
}

// banana.js
module.exports = {
    name: 'banana',
    color:'yellow'
}

// orange.js
module.exports = {
    name: 'orange',
    color:'orange'
}



we then create an index.js file in fruits directory 

and write

const apple = require('./apple');
const banana = require('./banana');
const orange = require('./orange');

let fruits = [apple,banana,orange];

module.exports = fruits;



now inside script.js in root directory

const info = require('./fruits');

console.log(info);



this will allow u to work with multiple js files 

index.js is like main() of Java we need an index.js to access

----------------------------------------------------------------------------


NPM (Node Package Manager)

npm is a standard package manager for Node.js

- library of packages
- command line tool 
- preinstalled in node


------------------------------------------------------------------------------------


Installing packages

npm install <package name>


node_modules : The node_modules folder contains every installed dependency for ur project

package-lock.json : It records the exact version of every installed dependence, including its sub-dependencies and their versions

package.json - this file contains descriptive and functional metadata about a project, such as a name, version and dependencies 

we can use package.json only to download dependencies ... in production we delete node modules , also if its deleted it can still be restored

we type npm install to restore it if we have package.json 


----------------------------------------------------------------------------------------------------------------------------------------------

Local v/s Global 

TO access a package globally 

1.sudo chown -R $USER /usr/local/lib/node_modules  

2.npm install -g <package name>

3.npm link <package name>

------------------------------------------------------------------------------------------------------------------

Require v/s Import

in file from which u want a fn

export const PI = 3.14

to the file u want to execute


import {sum,PI} from "./math.js"
console.log(sum(1,2))

in directory of the main file do npm init and create a package json 

in package.json create a key-value pair of  "type" : "module"; (do for import export files)

we cant selectively load only the pieces we need with require but with import,
we can selectively load only the pieces we need, which can save memory

Loading is synchronous for 'require' but can be asynchronous for 'import'


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


ExpressJS


Library 

A library is a collection of pre-written code that can be used to perform specific tasks

ex- Axios

framework

A framework is a set of pre-written code that provides a structure for developing software applications

ex- Express 

-----------------------------------------------------------------------------------------------------------

Express

-A Node.js web application framework that helps us to make web applications 
-It is used for server side Programming
-It listens the incoming requests
-parse krta hai request js k hisab se
-to match response with routes 


------------------------------------------------------------------------------------------------------------

Getting started with ExpressJS 

const express = require('express');
const app = express();


// console.dir(app);

let port = 3000;

app.listen(port,()=>{
    console.log(`app is listening on port : ${port}`);
});


# ports are the logical endpoints of a network connection that is used to exchange information between a web server and a web client


-------------------------------------------------------------------

app.use

app.use((req,res)=>{
    console.log('new incoming req');
});


app.use((req,res)=>{
    console.log(req)
    console.log('new incoming req');
    res.send('this is a basic response');
});


----------------------------------------------------------------------------


Routing 

It is a process of selecting a path for traffic in a network or between or across multiple networks

app.get("/apple", (req,res)=>{
    res.send({
        name:"apple",
        color:"red",
    });
});


// ek baar me ek hi response

app.get('/',(req,res)=>{
    res.send('u contacted root');
})

app.get("*",(req,res)=>{
    res.send('no page available')
})

--------------------------------------------------------------------

Nodemon - used to automatically restart server with code changes

npm i -g nodemon  -> install

nodemon -v -> check version

nodemon index.js -> run server


------------------------------------------------------------------------------

Path parameters

req.params 

app.get("/ig/:username",(req,res)=>{
    let {username} = req.params;
    res.send(`This account belongs to @${username}`);
});



Query Parameter 

// query parameter
app.get('/search',(req,res)=>{
    console.log(req.query);
    res.send('success');
})

http://localhost:3000/search?q=apple&color=green


 ------------------------------------------------------------------------------------------


 Templating

 EJS (Embedded Javascript Templates)

 EJS is a simple templating language that leets you generate HTML markup with plain Javascript

 Same as a component in ReactJS

 Just like Djinja in Django

npm init -y

npm i express

npm i ejs 

app.set('view engine','ejs') --> used to help render the template



app.set('view engine','ejs');

app.get('/',(req,res)=>{
    res.render('home.ejs');
})

app.get('/hello',(req,res)=>{
    res.send('hello');
})

app.listen(port,()=>{
    console.log(`listening on port ${port} `)
})


it is same as writing views.py and using djinja at the same time

EJS k folder k andar terminal rkhoge toh hi chlega 

agar global backend scale pe chahiye toh ye krna padega :

const path = require('path')

app.set("views",path.join(__dirname, "/views"))


----------------------------------------------------------------------------------

Interpolation syntax

Interpolation refers to embedding expressions into marked up text 


<%= 1+ 2 %> --> gives 3 , <%=%> --> gives output of a javascript function


-------------------------------------------------------------------------------------

Passing Data to EJS 


1st way 


rolldice.ejs

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Dive gave value:
        <%= Math.floor(Math.random()*6) + 1 %>
    </h1>
</body>
</html>



index.js

app.get('/rolldice',(req,res)=>{
    res.render('rolldice.ejs')
})



2nd way 

rolldice.ejs

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Dive gave value:
        <%= num %>
    </h1>
</body>
</html>

index.js

app.get('/rolldice',(req,res)=>{
    let diceVal = Math.floor(Math.random()*6)+ 1;
    res.render('rolldice.ejs',{num: diceVal}) --> key value pair object send 
})

we can also pass diceVal directly 

app.get('/rolldice',(req,res)=>{
    let diceVal = Math.floor(Math.random()*6)+ 1;
    res.render('rolldice.ejs',{diceVal}) --> key value pair object send 
})



---------------------------------------------------------------------------------------------------


Instagram EJS 

Create a basic template for instagram page based on following route :

/ig/:username 

insta.ejs

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram</title>
</head>
<body>
    <h2>This page belongs to <%= username %> </h2>
    <button>follow</button>
    <button>message</button>
</body>
</html>

index.js

app.get('/ig/:username',(req,res)=>{
    let { username } = req.params;
    res.render('instagram.ejs',{username})
})
------------------------------------------------------------------------

Conditional Statements in EJS (<%   %>)

ex. 

<% if (diceVal == 6) { %>
<h2>Nice! Roll dice again.</h2>
<% } %>

------------------------------------------------------------
Loops in EJS 

<% for( user of followers) { %>
<li><%= user %></li>
<% } %>


-------------------------------------------------------------------

Instagram Page with EJS 

const instaData = require('./data.json')

instagram.ejs 

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram</title>
    <style>
        img {
            height:100px;
            width: 100px;
        }
    </style>
</head>
<body>
    <h2>This page belongs to <%= data.name %> </h2>
    <button>follow</button>
    <button>message</button>

    <p>Followers: <%= data.followers %>&nbsp;&nbsp;&nbsp;&nbsp; Following : <%= data.following %></p>
    <hr>
    <% for(let post of data.posts) {%>
        <img src="<%= post.image %>" alt="some img">
        <p>
            Likes: <%= post.likes %>&nbsp;&nbsp;&nbsp;&nbsp; comments : <%= post.comments %>
        </p>
    <% } %>
</body>
</html>

index.js

app.get('/ig/:username',(req,res)=>{
    let { username } = req.params;
    const instaData = require('./data.json')
    const data = instaData[username]
    if(data){
        res.render('instagram.ejs',{data})
    }
    else{
        res.render('error.ejs')
    }
    
})

error.ejs 

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>No Account</title>
</head>
<body>
    <h1>No account formed</h1>
</body>
</html>

------------------------------------------------------------------------------------------------

Serving Static Files for css and js files 

in EJS folder

app.use(express.static(folder_name))

in Global backend 

app.use(express.static(path.join(__dirname, 'public')));

----------------------------------------------------------------------------------

Includes 

create sub templates -> link to a new page 


<%- include('includes/head.ejs'); %>


the html boiler plate is everywhere ..... make a folder in views called includes, inside it create head.ejs and paste the boiler code

now remove the code of same boiler plate and replace it with include('includes/head.ejs') %>

this can also be used when creating a navbar or sidebar for management systems 

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


Miscellaneous 


GET & POST requests 

GET 

- used to GET some response 
- data sent in query strings (limited, string data & visible in URL)
- GET ka scene hai ki username password uppar url k query me jayega jo compromise krskta h user ko usse accha POST use kro
POST 

- used to POST something (for Create/Write/Update)
- Data sent via request body (any type of data)


frontend/index.html

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GET and POST request</title>
</head>
<body>
    <h1>GET response</h1>
    <form action="http://localhost:8080/register" method="get">
        <input placeholder="enter username" type="text" name="user">
        <input placeholder="enter password" type="password" name="password">
        <button>submit</button>
    </form>
    <hr>
    <h1>POST response</h1>
    <form action="http://localhost:8080/register" method="post">
        <input placeholder="enter username" type="text" name="user">
        <input placeholder="enter password" type="password" name="password">
        <button>submit</button>
    </form>
</body>
</html>


backend/index.js

const express = require('express')
const app = express()
const port = 8080


app.get('/register',(req,res)=>{
    let {user, password} = req.query;
    res.send(`standard GET response. Welcome ${user}`)
})


app.post('/register',(req,res)=>{
    res.send('standard POST response')
})

app.listen(port,()=>{
    console.log(`listening to post ${port}`)
})




-----------------------------------------------------------------------

Handling Post Requests 

Set up POST request route to get some response 

Parse POST request data 

app.use(express.urlencoded({extended : true }));
app.use(express.json());



we just cant use this to print the body 

app.post('/register',(req,res)=>{
    console.log(req.body)
    res.send('standard POST response')
})


express doesnt understand human string thats why we parse it using encoding 

app.use(express.urlencoded({extended : true }));
app.use(express.json());  --> if we send this POST request an JSON it can read using this parser else it will say undefined

app.post('/register',(req,res)=>{
    console.log(req.body)
    let {user, password} = req.body;
    res.send(`standard POST response. Welcome ${user}`)
})

now this works 


---------------------------------------------------------------------------------------

Revisiting JS (OOPS)


Object Oriented Programming

TO structure our code 

- prototypes
- New Operator
- constructors
- classes 
- keywords (extends,super)


-------------------------------------------------------------------

Object Prototypes 

Prototypes are the mechanism by which Javascript objects inherit features from one another.
It is like a single template object that all objects inherit methods and properties from without having their own copy 


arr__proto__(reference)

Array.prototype(actual object)

String.prototype 

------------------------------------------------------------------------------------------------

Factory Functions 

A function that creates objects 

function PersonMaker(name,age){
    const person = {
        name: name,
        age: age,
        talk: function(){
            console.log(`hi my name is ${this.name}`)
        },
    };
    return person;
}


disadvantage = har obj k paas khud ki copy jaati hai 

like 

let p1 = PersonMaker('adam',23)
let p2 = PersonMaker('eve',22)

p1.talk() === p2.talk() --> false as both obj have their own copy 


---------------------------------------------------------------------------------------

New Operator 

THe new operator lets developers create an instance of a user-defined object type or of one of the built-in object types that has a constructor function 

// constructors -> doesnt return anything and start with capital letter 
function Person(name,age){
    this.name = name;
    this.age = age;
}

Person.prototype.talk = function() {
    console.log(`hi my name is ${this.name}`)
};


let p1 = new Person('adam',23)
let p2 = new Person('eve',22)


It also changes "this" inside the constructor function 

-------------------------------------------------------------------------------------------

Classes 

Classes are a template for creating objects 

The constructor method is a special method of a class for creating and initializing an object instance of that class

class Person{
    constructor(name,age){
        this.name = name;
        this.age = age;
    }

Person.prototype.talk = function() {
    console.log(`hi my name is ${this.name}`)
};
}


let p1 = new Person('adam',23)
let p2 = new Person('eve',22)

still p1.talk === p2.talk --> true cauz of same copy
----------------------------------------------------------------------------------------------------


Inheritance

Inheritance is a mechanism that allows us to create new classes on the basis of already existing classes 


class Student extends Person{
    constructor(name,age,marks){
        super(name,age);
        this.marks = marks;
    }

    greet(){
        return "hello"
    }
}


let s1 = new Student('adam',21,90);

-----------------------------------------------------------------------------------------------------------------------

REST 

Representational State Transfer 

REST is a architectural style that defines a set of constraints to be used for creating web services 

CRUD operations

GET retrieves resources 
POST submits new data to the server 
PUT updates existing data 
PATCH updates existing data partially 
DELETE removes data 

Creating RESTful APIs 

GET  /posts          to get data for all posts   INDEX(Main cheez)

POST /posts          to add new post             CREATE

GET  /posts/:id      to get one post (using id)  VIEW

PATCH /posts/:id     to update specific post     UPDATE

DELETE /posts/:id    to delete specific post     DESTROY



Redirect 

Express can redirect to other pages by using : res.redirect(URL)

Create ID for posts 

UUID package 

Universally unique identifier 

npm i uuid 


html forms can only send GET and POST request 

to sent PUSH , DELETE , PATCH we use method-override package from npm


+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


WHat is a Database 

It is a collection of data in a format that can be easily accessed 


Why databases ?

-can store large data 
-features like security, scalability,etc.
-easier to insert, update or delete data 

SQL  - relational database(data stored in tables) ex. MySQL, Oracle, PostgreSQL, etc. 

NoSQL - Non Relational Database (data stored in document/key-val/graphs,etc) ex. MongoDB, Cassandra, Neo4j,etc

SQL (Structured Query Language)

SQL is a programming language used to interact with relational databases 


{see screenshots }

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


Faker 

To generate fake data 

let getUser = () =>{
    return [
        faker.string.uuid(),
        faker.internet.userName(),
        faker.internet.email(),
        faker.internet.password(),
    ];
};

----------------------------------------------------------------

MySQL2 Package 

To connect Node with MySQL 

connection.end(); // to close connection 

To establish connection 

npm i mysql2 

const mysql = require('mysql2')

const connection = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    database: 'test'
});


TO query a statement 


try{
    connection.query('SHOW TABLES;',(err,result)=>{
        if(err) throw err;
        console.log(result)
    })
}catch(err){
    console.log(err)
}



There is also a way to run a sql script via a file 


1. make file with extension .sql 
2. open sql cmd by writing "sql -u root -p"
3. write source 'file name'

--------------------------------------------------------------------------


 random 100 entries inside db 

 const {faker } = require('@faker-js/faker');
const mysql = require('mysql2')


const connection = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    database: 'delta_app',
    password: 'abc123'
});

let q ='INSERT INTO user (id,username,email,password) VALUES ?'

let getUser = () =>{
    return [
        faker.string.uuid(),
        faker.internet.username(),
        faker.internet.email(),
        faker.internet.password(),
    ];
};

let data=[];
for(let i=1; i<=100; i++)
{
    data.push(getUser());
}

try{
    connection.query(q,[data],(err,result)=>{
        if(err) throw err;
        console.log(result)
    })
}catch(err){
    console.log(err)
}

connection.end();


------------------------------------------------------------------------

MongoDB 

mongosh // to start 

use college // to create & use a new db called "college" 

db // to see which db u in 

-----------------------------------------------------------------------------------------------------------------------------------------------
BSON data 

JSON is Text based and parsing is hard and memory storage is inefficient 
BSON is Binary based and MongoDB takes JSON which appears as text and converts them to Binaries which is parsed easily and is efficient for storage 

---------------------------------------------------------------------------------------------------------------

Collections 

Document : Mongo stores data in form of documents (BSON docs)
Collection  : MongoDB stores documents in collection 

ek row -> ek document 

----------------------------------------------------------------------------------------------------------------

Insert One 

test> use college

college> show collections

college> db.student.insertOne({name:'adam', age: 19, marks:80})

college> db.student.find()
[
  {
    _id: ObjectId('67610a7d2dad56442cea523b'),    name: 'adam',
    age: 19,
    marks: 80
  }
]

------------------------------------------------------------------------------------------------

Insert Many 

insertMany(array of docs)

db.student.insertMany([{name:'bob',marks:90},{name:'eve',city:'delhi'}])

-----------------------------------------------------------------------------------------------

Find in DB (same as SELECT in student)

db.collection.find()  //returns everything

for specific queries 

db.collection.find({key:value}) --> returns cursor --> array k andar objects 

db.collection.findOne({key:value}) --> returns single document --> sirf objects 


Find student where marks > 75 

db.student.find({marks:{$gt:75}})

Find students who live in delhi or mumbai 

db.student.find({city:{$in:['delhi','mumbai']}})

less than --> $lt 
less than equal to --> $lte 
greater than --> $gt 
greater than equal to --> $gte 
$ne --> not equal tp 
not in array --> $nin 

$and 
$not 
$nor 
$or 
$exists 
$type 


--------------------------------------------------------------------

UPDATE in DB 

updateOne
updateMany
replaceOne

college> db.college.updateOne({name:'raju'},{$set:{marks:100}})
{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}
college> db.college.find()
[
  {
    _id: ObjectId('67613852e95fcff13586b334'),
    name: 'raju',
    city: 'thane',
    marks: 100
  },
  {
    _id: ObjectId('67613852e95fcff13586b335'),

college> db.college.updateOne({name:'raju'},{$set:{marks:98}})
{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}
college> db.college.find()
[
  {
    _id: ObjectId('67613852e95fcff13586b334'),
    name: 'raju',
    city: 'thane',
    marks: 98
  },
  {
    _id: ObjectId('67613852e95fcff13586b335'),
    name: 'ava',
    city: 'amsterdam',
    marks: 90
  }
]
college> db.college.updateMany({marks:{$gt:90}},{$set:{city:"mumbai"}})
{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}
college> db.college.find()
[
  {
    _id: ObjectId('67613852e95fcff13586b334'),
    name: 'raju',
    city: 'mumbai',
    marks: 98
  },
  {
    _id: ObjectId('67613852e95fcff13586b335'),
    name: 'ava',
    city: 'amsterdam',
    marks: 90
  }
]
college> db.college.replaceOne({name:'ava'},{name:'avanti',marks:98})
{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}
college> db.college.find()
[
  {
    _id: ObjectId('67613852e95fcff13586b334'),
    name: 'raju',
    city: 'mumbai',
    marks: 98
  },
  {
    _id: ObjectId('67613852e95fcff13586b335'),
    name: 'avanti',
    marks: 98
  }
]
college> db.college.updateOne({name:'avanti'},{$set:{city:'mumbai'}})
{
  acknowledged: true,
  insertedId: null,
  matchedCount: 1,
  modifiedCount: 1,
  upsertedCount: 0
}
college> db.college.find()
[
  {
    _id: ObjectId('67613852e95fcff13586b334'),
    name: 'raju',
    city: 'mumbai',
    marks: 98
  },
  {
    _id: ObjectId('67613852e95fcff13586b335'),
    name: 'avanti',
    marks: 98,
    city: 'mumbai'
  }
]

--------------------------------------------------------------------------------

Nesting 

college> db.college.insertOne({name:'sai', age: 19, performance:{grade:'o',marks:90}})

college> db.college.find({'performance.marks':90})
[
  {
    _id: ObjectId('67613cd1e95fcff13586b336'),
    name: 'sai',
    age: 19,
    performance: { grade: 'o', marks: 90 }    
  }
]

-----------------------------------------------------------------------------------------------



Delete in DB 



deleteOne 

college> db.college.deleteOne({city :{$in:['mumbai']}})
{ acknowledged: true, deletedCount: 1 }
college> db.college.find()
[
  {
    _id: ObjectId('67613cd1e95fcff13586b336'),
    name: 'sai',
    age: 19,
    performance: { grade: 'o', marks: 90 },   
    marks: 'thane'
  }
]

deleteMany

college> db.college.deleteMany({'performance.marks' :{$lte:97}})
{ acknowledged: true, deletedCount: 2 }
college> db.college.find()
[
  {
    _id: ObjectId('67616c1fe95fcff13586b338'),
    name: 'rau',
    age: 19,
    performance: { grade: 'o', marks: 100 }   
  }
]

Truncate - dont give any parameter inside deleteMany

college> db.college.deleteMany({})
{ acknowledged: true, deletedCount: 1 }
college>

drop table 

first truncate then drop 

college> db.dropDatabase()
{ ok: 1, dropped: 'college' }


----------------------------------------------------------------------

Mongoose 

A library that creates a connection between MongoDB and Nodejs JS runtime env 

It is an ODM (Object Data Modelling) library 

run mongod in cmd 
run mongosh in other 
in folder run npm init -y 
npm i mongoose 

starter code 

const mongoose = require('mongoose')


//async process it returns promise 
//mongoose.connect('mongodb://127.0.0.1:27017/test')

main().then(()=> console.log('connection successful'))
.catch(err=> console.log(err))

async function main(){
    await mongoose.connect('mongodb://127.0.0.1:27017/test')

}

-------------------------------------------------------------------------------------

Schema 

Schema defines the shape of the documents within that collection 

const userSchema = new mongoose.Schema({
    name: String,
    email: String,
    age: Number,
});

---------------------------------------------------------------------------------------

Models 

Model in mongoose is a class with which we construct documents 
write model name singular  (ex. user and not users)

const User = mongoose.model("User",userSchema);

after this run the file using "node index.js" and go to the mongosh terminal and enter "show collections"
u will see a collection named "users" 

if we made model Product we would have got products 

User -> users 
Product -> products 

---------------------------------------------------------------------------------------

insert 

inserting one 

const user1 = new User({name:'adam',email:'adam@yahoo.in',age:48});

const user2 = new User({name:'eve',email:'eve@yahoo.in',age:48});

user1.save() // save in DB 
user2.save() // save in DB 


Order Matters:
Define the schema, then the model, and only after that create and save an instance.


const mongoose = require('mongoose');

// Connect to MongoDB
main()
  .then(() => console.log('Connection successful'))
  .catch((err) => console.log(err));

async function main() {
  await mongoose.connect('mongodb://127.0.0.1:27017/test');
}

// Define Schema
const userSchema = new mongoose.Schema({
  name: String,
  email: String,
  age: Number,
});

// Define Model
const User = mongoose.model("User", userSchema);

// Create Instance
const user1 = new User({ name: 'adam', email: 'adam@yahoo.in', age: 48 });

// Save to Database
user1.save()
  .then(() => console.log('User saved successfully'))
  .catch((err) => console.log('Error:', err));


INSERT MULTIPLE 

User.insertMany(
    [
        {name:'tony',email:'tony@gmail.com',age:50},
        {name:'bruce',email:'bruce@gmail.com',age:45},
        {name:'peter',email:'peter@gmail.com',age:56},
    ]
).then((data)=>{
    console.log(data)
});

Note - Mongoose uses Operation Buffering 

Mongoose lets you start using your models immediately, without waiting for mongoose to establish a connection to MongoDB 


-----------------------------------------------------------------------------------------------------------------------------------

FIND 

Model.find() // returns a query object thennable 

Mongoose queries are not promises but they have a .then()


User.find({age:{$gte:47}}).then((data)=>{
    console.log(data)
});

to return a single redult which comes first 

User.findOne({age:{$gte:47}}).then((data)=>{
    console.log(data)
});


---------------------------------------------------------------------------------


UPDATE Model -> User 

User.updateOne({name:'bruce'},{age:49}).then((res)=> console.log(res))

User.updateMany({age:{$gt: 45}},{age:49}).then((res)=> console.log(res))

Model.findOneAndUpdate()  pehle dhund fir update kr 
User.findOneAndUpdate({name:'tony'},{age:60},{new:true}).then((data)=>console.log(data))

Model.findByIdAndUpdate() same as above 
User.findByIdAndUpdate({_id:'676179a987c10ebb50e5277c'},{age:60},{new:true}).then((data)=>console.log(data))


if we dont write "new: true"  then it returns modified document 

----------------------------------------------------------------------------------------------------------------------------

DELETE 

these both methods return count of deleted documents 

User.deleteOne({name:'Adam'}).then((res)=>{
    console.log(res);
});

User.deleteMany({age:{$gt:40}}).then((res)=>{
    console.log(res);
});

TO print what is deleted use these methods 

Model.findByIdAndDelete() -> give _id

Model.findOneAndDelete() 

--------------------------------------------------------------------------

Schema Validations 

Basically rules for schema 

one way to initialize a schema is 

const bookSchema = new mongoose.Schema({
    title: String,
    autor: String,
    price: Number,
});


Other is (user for schema type conditions)

const bookSchema = new mongoose.Schema({
    title: {
        type: String,
        required: true,

    },
    autor: {
        type: String,
        
    },
    price: {
        type: Number,
        
    },
});


const Book = mongoose.model('Book',bookSchema);

------------------------------------------------------------------------------

Schema Type Options 

const bookSchema = new mongoose.Schema({
    title: {
        type: String,
        required: true,
        maxLength:20,

    },
    autor: {
        type: String,
        
    },
    price: {
        type: Number,
        min:1,
        
    },
    discount:{
        type: Number,
        default:0
    },
    category:{
        type:String,
        enum:['fiction','non-fiction']
    },
    genre: [String] --> Array of Strings 
});


Schema pe jo jo rules likhe hai vo sirf insertion pe apply hoga update pe nhi 


agar update par bhi rules apply krne hai toh : 

{runValidators:true} --> in update statement 

Book.findByIdAndUpdate('id',{price:-100},{runValidators:true})


we can also give custom errors if rules arent followed by providing in array 


const bookSchema = new mongoose.Schema({
    title: {
        type: String,
        required: true,
        maxLength:20,

    },
    autor: {
        type: String,
        
    },
    price: {
        type: Number,
        min:[1,'Price is too low'],
        
    },
    discount:{
        type: Number,
        default:0
    },
    category:{
        type:String,
        enum:['fiction','non-fiction']
    },
    genre: [String] --> Array of Strings 
});


Handling Errors 

To console the error in log we do 

console.log(err.errors.category.properties.message);

----------------------------------------------------------------------

Mongo with Express

Creating the Model 

see MONGO3

chat will have id , from , to , message , created_at

Using date in ejs 

<%= chat.created_at.toString().split('')[4] %>
<%= chat.created_at.toString().split(''),slice(0,4).join('')%>

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Middleware 
It can access req and res of the function 
chaining happens -> ek middleware dusre middleware ko control de 
It can send response and stop the chaining 

see middleware image

Common Middleware functions 

methodOverride 
bodyParser
express.static
express.urlencoded 

---------------------------------------------------------------------------

What do middlewares do 

- execute any code 
- make changes to the request and response objects 
- end the request-response cycle
- call the next middleware function in the stack 


---------------------------------------------------------------------

Our 1st middleware 

app.use(middleware) 


app.use((req,res)=>{ //middleware
    console.log('hi im middleware')
    res.send('bye bye')
})
app.get('/',(req,res)=>{
    res.send('hi im root')
})

app.get('/render',(req,res)=>{
    res.send('hi this is the rendered page')
})
app.listen(port,()=>{
    console.log(`connected to port ${port}`)
})



if we dont write res.send inside middleware then all paths wont load ... if we send a res we will only get the same response no matter the path 
-------------------------------------------------------------------------------------------------------------------------------------------------------

using Next

next middleware is commonly denoted by variable called next 

app.use((req,res,next)=>{
    console.log('Time:',Date.now())
    next()
})

If the current middleware fn does not end the req-res cycle, it must call next() to pass control to the next middleware fn 

const express = require('express')
const app = express();
const port = 8080 

app.use((req,res,next)=>{
    console.log('hi im middleware')
    return next()
})
app.use((req,res,next)=>{
    console.log('hi im  2nd middleware')
    return next()
})
app.get('/',(req,res)=>{
    res.send('hi im root')
})

app.get('/render',(req,res)=>{
    res.send('hi this is the rendered page')
})
app.listen(port,()=>{
    console.log(`connected to port ${port}`)
})

-------------------------------------------------------------

Creating Utility Middleware 

Logger 

app.use((req,res,next)=>{
    req.responseTime = new Date(Date.now()).toString();
    console.log(req.method, req.path, req.responseTime, req.hostname);
    next();
})

middleware code k start me likhte require k baad sab 

middle pe aata h 
-----------------------------------------------------------------------------------

app.use() callback

applications : can implement auth 


app.use((req,res)=>{
    res.status(404).send('page not found')
})

-------------------------------------------------------------------------------------

Activity : API token as a query String 

app.use('/api',(req,res,next)=>{
    let {token} = req.query
    if(token === 'get access'){
        next();
    }
    res.send('ACCESS DENIED')
})
app.get('/api',(req,res)=>{
    res.send('data')
})


-------------------------------------------------------------------

Multiple Middlewares 

const checkToken = (req,res,next)=>{
    let {token} = req.query
    if(token === 'get access'){
        next();
    }
    res.send('ACCESS DENIED')
}
app.get('/api',checkToken,(req,res)=>{
    res.send('data')
})

we can pass multiple middlewares like this 

------------------------------------------------------------------------

Handling Errors 

Express Default Error Handler 

app.get('/wrong',(req,res)=>{
    abcd = abcd // gives error by giving stack trace of errors 
})

---------------------------------------------------------------------------------

Error Handlers 

app.use((err,req,res,next)=>{
    console.log('----------ERROR-----------')
    next(err)
})


ExpressError class in ExpressError.js 

class ExpressError extends Error{
    constructor(status,message){
        super()
        this.status=status
        this.message = message
    }
}

module.exports = ExpressError


in app.js 

const checkToken = (req,res,next)=>{
    let {token} = req.query
    if(token === 'get access'){
        next();
    }
    throw new ExpressError(401,'Fuck u')
}

app.use((err,req,res,next)=>{
    let {status,message}=err;
    res.status(status).send(message)
})

---------------------------------------------------------

we assume defaults cauz sometime status may be undefined resulting in breaking of backend
so we assume 500 as status with some msg 

app.use((err,req,res,next)=>{
    let {status=500,message='some error occured'}=err;
    res.status(status).send(message)
})

----------------------------------------------------------------------------------------

Error Class 

Create an admin route and send an error with a 403 status code 

order matters --> error middleware should be at last 

app.use((err,req,res,next)=>{
    let {status=500,message='some error occured'}=err;
    res.status(status).send(message)
})

app.get('/admin',(req,res)=>{
    throw new ExpressError(403,'access is forbidden')
})

-------------------------------------------------------------------------------

Handling Asyn Errors 

// Route for fetching a chat by ID
app.get('/chats/:id', async (req, res, next) => {
    const { id } = req.params;

    // Check if the provided ID is a valid MongoDB ObjectId
    if (!mongoose.isValidObjectId(id)) {
        return next(new ExpressError(400, 'Invalid chat ID'));
    }

    try {
        // Attempt to find the chat in the database
        const chat = await Chat.findById(id);
        if (!chat) {
            return next(new ExpressError(404, 'Chat not found'));
        }
        res.render('edit.ejs', { chat });
    } catch (err) {
        next(err); // Pass any unexpected errors to the error-handling middleware
    }
});

inside next we call the class in an async function 

-----------------------------------------------------------------------------------------

using wrapAsync (not bulky as try-catch)

it is a fn which has argument as function and which returns function 

function asyncWrap(fn){
    return function(req,res,next){
        fn(req,res,next).catch((err) => next(err));
    }
}

// Route for fetching a chat by ID
app.get('/chats/:id', asyncWrap( async (req, res, next) => {
    const { id } = req.params;

    // // Check if the provided ID is a valid MongoDB ObjectId
    // if (!mongoose.isValidObjectId(id)) {
    //     return next(new ExpressError(400, 'Invalid chat ID'));
    // }

    // try {
        // Attempt to find the chat in the database
        const chat = await Chat.findById(id);
        if (!chat) {
            return next(new ExpressError(404, 'Chat not found'));
        }
        res.render('edit.ejs', { chat });
    // } catch (err) {
    //     next(err); // Pass any unexpected errors to the error-handling middleware
    // }
}))

----------------------------------------------------------------------------------------------------

How to handle errors in production 

const handleValidationError= (err) => {
    console.log('this is a validation error please follow rules')
    console.dir(err.message)
    return err
}
app.use((err,req,res,next)=>{
    console.log(err.name)
    if(err.name === 'ValidationError')
        handleValidationError(err)
    next(err)
})

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Form Validations 

When we enter data in the form, the browser and/or the web server will check to see that the data is in the correct format and within the constraints set by the application 


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Handling Deletion 

using Mongoose Middleware 

We can use 2 Middlewares 

- Pre-run before the query is executed 
- Post-run after the query is executed 


If we remove a user we also need its posts to be deleted as well ..... its more like a procedure and rollback of SQL 

findByOneAndDelete() is a middleware which executes even when we call findByIdAndDelete ...... the findByOneAndDelete is basically a middleware 
------------------------------------------------------------------------------------------------------------------------------


{nameOfSchema}.pre('findOneAndDelete', async()=>{
    console.log('pre middleware')
})

{nameOfSchema}.post('findOneAndDelete', async()=>{
    console.log('post middleware')
})

--------------------------------------------------------------------

DELETING REVIEWS (PROJECT) 

$pull operator in mongoose 

The $pull operator removes from an existing array all instances of a value or values that match a specified condition 

------------------------------------------------------------------------------------------------------------------------
Express Router 

Express router are a way to organize your express application such that our primary app.js file does not become bloated 

const router = express.Router() // creates new router object 

create a folder named routes 

suppose u have functionality of CRUD for users and posts .... rather than creating 8 different methods in a single server.js file 

we can create two files inside routes named user.js and post.js 

inside user.js all the user related methods will be present 

inside post.js all the post related methods will be present 

inside user and post ... write const router = express.Router() --> to create a router object 

now inside this user and post files ..... we know routes are written as app.get(), app.post()

also where /user/:id , /user/ , /post type of common words will be present in both the user and post files remove them 

replace app with router

then do module.exports = router 

....

inside the server.js file (i.e app.js)
do 

const users = require('./routes/user.js)

app.use('/users',users)

const posts = require('./routes/post.js)

app.use('/posts',posts)

to use this routes 

THis will make code shorter on the main server file 

---------------------------------------------------------------------------------

when we do routing ensure to give a parameter called mergeParams set to true 
for when the id is getting undefined (we did gpt but later disclosed by shraddha di to use that)

router = express.Router({mergeParams : true})

mergeParams -> Preserve the req.params values from the parent router 
 
if the parent and the child have conflicting parent names, the child's value takes precedence

------------------------------------------------------------------------------------------------------------

Cookies 

Web cookies - HTTP cookies are small blocks of data created by a web server while a user is browsing a website and 
placed on the user's computer or other device by the user's web browser 

How to send ?? 

-> use "res.cookie(name,value)" like res.send , res.render , res.redirect 

ex . app.get('/getCookies',(req,res)=>{
    res.cookie('greet','namaste')
})


How to receive ??


-> install coookie parser from npm -> npm i cookie-parser 

then do const cookieParser = require('cookie-parser')

and then do 

app.use(cookieParser())


after that in any url u want to receive the cookie just request them 

ex. app.get('/',(req,res)=>{
    console.dir(req.cookies)
    res.send('hi, im root')
})


so first the cookies will be parsed by the middleware and then passed on the urls which requested it

some applicaions -

app.get('/greet', (req,res)=>{
    let {name = 'defaultName'} = req.cookies
    res.send(`hi ${name}`)
})


But hackers can exploit it by going to console and changing the key value pairs of cookies 


for that we have something called 

"Signed Cookies"

example - 

app.use(cookieParser('secretcode'))

app.get('/getSignedCookies', (req,res)=>{
    res.cookie('made-in','india', {signed: true})
    res.send('signed cookie sent')
})


app.get('/verify',(req,res)=>{
    console.log(req.signedCookies)
    res.send('verified')
})

it will give the correct cookie in key value pair if not tampered 

req.cookies and req.signedCookies are different 

if hackers tamper with the cookie it returns empty object

if they intelligently remove and add their text still the output key will be printed but its key will be false



++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


What is State 

Stateful Protocol 

Stateful protocol require server to save the status and session information 

ex - ftp 

Stateless protocol 

Stateless protocol does not require the server to retain the server information 

ex - http 

---------------------------------------------------------------------------------------------

Express sessions 

An attempt to make our session stateful 


ex- amazon pe u add all the items to card without login , when u proceed to pay ; it asks u to login and after login ur cart remains the same - stateful 

npm i express-session 

const session = require('express-session')

app.use(session({secret : 'supersecretstring'}))

app.get('/test',(req,res)=>{
    res.send('successful')
})

here when we come to the test route we can see a session id is generated in the cookies section of application under the inspect tab of browser \

the session comes with options then 

app.use(session({secret : 'supersecretstring', resave: false, saveUninitialized: true}))


resave : kuch change nhi hua tab bhi resave hoga session store , by default false rehta h
saveUninitialized: jo session initialise bhi nhi hua vo bhi isme store hoga , by default true 

ab depricated warning nhi aayegi 


we can store session options in a variable and then use that 

const sessionOptions = {
    secret : 'supersecretstring',
    resave: false, 
    saveUninitialized: true
}

app.use(session(sessionOptions))

app.get('/register', (req,res)=>{
    let { name = 'anonymous' } = req.query // default is anonymous
    req.session.name = name  // storing the name obtained in query string in session 
    res.redirect('/hello')
})


app.get('/hello', (req,res)=>{
    res.send(`hello, ${req.session.name}`) // using session name 
})


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


connect-flash 

The flash is a special area of the session used for storing messages. Messages are written to the flash and cleared after being displayed to the user 

npm i connect-flash // often used with redirects 

we need session , view.ejs for flashing messages 

app.use(flash())


app.get('/register', (req,res)=>{
    let { name = 'anonymous' } = req.query // default is anonymous
    req.session.name = name  // storing the name obtained in query string in session 
    req.flash('success','user registered successfully')
    res.redirect('/hello')
})


app.get('/hello', (req,res)=>{
    res.render('page.ejs', { name : req.session.name, msg : req.flash('success')}) 
})


----------------------------------------------------------------------------------------------------------

using locals for flashing messages 


app.get('/hello', (req,res)=>{
    res.locals.messages = req.flash('success')
    res.render('page.ejs', { name : req.session.name}) 
})


page.ejs 


<%= messages %>



++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Authentication is the process of verifying who someone is 

Authorization is the process of verifying what specific applications , files , and data a user has access to 

-----------------------------------------------------------------------------------------------------------------------

Hashing 

For every input there is a fixed output 

They are one-way functions, we can't get input from output 

for a different input, there is a different output but of same length 

Small changes in input should bring large changes to the output 

---------------------------------------------------------------------------------------------------------------------------------------------------------

Salting

Password Salting is a technique to protect passwords stored in databases by adding a string of 32 or more characters and then hashing them

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Passport

npm i passport 

npm i passport-local 

npm i passport-local-mongoose 


------------------------------------------------------------------

Configuring Strategy 

passport.initialize()

A middleware that initializes passport 


passport.session()

A web application needs the ability to identify users as they browse from page to page. This series of requests and responses, each associated with the same user, is known as a session.

passport.use(new LocalStrategy(User.authenticate()))

-------------------------------------------------------------------------------------------------------------------------------------------------------------

SignUP user

GET -> display form

POST -> submit form

-------------------------------------------------------------------

Login User

GET -> display form

POST -> submit form
--------------------------------------------------------

Connecting Login Route 

how to check if user is logged In?

req.isAuthenticated() //Passport method

-----------------------------------------------------------

Logout User 

GET /logout

router.get('/logout',(req,res)=>{
    req.logout((err) =>{
        if(err){
            return next(err)
        }
        req.flash('success','logged you out')
        res.redirect('/listings')
    })
})

-----------------------------------------------------------------------------

Login after SignUp 

Passport's login method automatically establishes a login session
We can invoke login to automatically login a user 

req.login(registeredUser,(err)=>{
    if(err){
        next(err)
    }
    req.flash('success','Welcome to Wanderlust')
    res.redirect('/listings')
})